{
  "name": "code-tree",
  "version": "3.1.0",
  "description": "Code tree sidebar for GitHub and GitLab",
  "scripts": {
    "start": "node build.js --watch",
    "watch": "node build.js --watch",
    "build": "node build.js",
    "dist": "node build.js --production --dist"
  },
  "license": "AGPL",
  "devDependencies": {
    "archiver": "^7.0.1",
    "chokidar": "^4.0.3",
    "esbuild": "^0.24.2",
    "less": "^4.2.1",
    "sharp": "^0.34.5"
  }
}
